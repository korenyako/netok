# 🎯 Пересмотренные приоритеты развития Netok

## Новая стратегия: Windows First!

### Философия:
**"Доделать одну платформу на 100%, чем три на 33%"**

---

## 📊 Текущая ситуация

### Windows (85% готовности):
- ✅ Wi-Fi (SSID, RSSI, тип подключения)
- ✅ DNS detection и настройка
- ✅ Gateway detection
- ✅ Internet connectivity check
- ⚠️ Router MAC address - TODO
- ⚠️ Router vendor (OUI lookup) - TODO
- ⚠️ Internet latency measurement - TODO

### Linux (30% готовности):
- ✅ Базовая диагностика
- ❌ Wi-Fi (stub)
- ⚠️ DNS/Gateway (частично)

### macOS (30% готовности):
- ✅ Базовая диагностика
- ❌ Wi-Fi (stub)
- ⚠️ DNS/Gateway (частично)

---

## 🔄 НОВЫЙ ПЛАН: Phase 2.5 → Windows Polish

### Phase 2.5: Windows Complete (1 неделя)

**Цель:** Windows на 100%

#### 2.5.1 Router MAC Address Lookup (4-6 часов)
**Приоритет:** 🟡 MEDIUM
**Локация:** `netok_core/src/lib.rs:414`

**Задачи:**
- [ ] Использовать `arp -a` или PowerShell `Get-NetNeighbor`
- [ ] Парсить ARP таблицу
- [ ] Извлечь MAC адрес роутера по IP
- [ ] Добавить тесты
- [ ] Обработать ошибки

**Реализация:**
```rust
#[cfg(target_os = "windows")]
fn get_router_mac(gateway_ip: &str) -> Option<String> {
    use std::process::Command;
    
    // PowerShell: Get-NetNeighbor -IPAddress <gateway>
    let output = Command::new("powershell")
        .args(&[
            "-NoProfile",
            "-Command",
            &format!("Get-NetNeighbor -IPAddress {} | Select-Object -ExpandProperty LinkLayerAddress", gateway_ip)
        ])
        .output()
        .ok()?;
    
    let mac = String::from_utf8_lossy(&output.stdout).trim().to_string();
    if !mac.is_empty() && mac.contains('-') {
        Some(mac.replace('-', ":").to_uppercase())
    } else {
        None
    }
}
```

#### 2.5.2 Router Vendor Lookup (OUI Database) (6-8 часов)
**Приоритет:** 🟢 LOW (nice-to-have)
**Локация:** `netok_core/src/lib.rs:415`

**Задачи:**
- [ ] Скачать OUI database (IEEE)
- [ ] Встроить в binary или загружать при первом запуске
- [ ] Парсить первые 3 байта MAC → vendor
- [ ] Кэшировать результаты
- [ ] Добавить тесты

**Опции:**
1. **Embedded CSV** - встроить маленькую базу (50KB)
2. **Online API** - macvendors.com/api (rate limited)
3. **Local cache** - скачать полную базу один раз

**Рекомендация:** Embedded CSV с топ-500 производителей

#### 2.5.3 Internet Latency Measurement (8-10 часов)
**Приоритет:** 🟡 MEDIUM
**Локация:** `netok_core/src/lib.rs:534`

**Задачи:**
- [ ] Измерить RTT для DNS запроса (1.1.1.1, 8.8.8.8)
- [ ] Измерить RTT для HTTP запроса (http://www.google.com)
- [ ] Взять среднее / минимум из 3 попыток
- [ ] Учесть таймауты
- [ ] Добавить тесты

**Реализация:**
```rust
fn measure_internet_latency() -> Option<u32> {
    use std::time::Instant;
    
    let mut latencies = Vec::new();
    
    // 3 попытки DNS запроса
    for _ in 0..3 {
        let start = Instant::now();
        if let Ok(_) = trust_dns_resolver::Resolver::default()
            .lookup_ip("www.google.com") {
            latencies.push(start.elapsed().as_millis() as u32);
        }
    }
    
    if latencies.is_empty() {
        return None;
    }
    
    // Возвращаем минимальную latency (лучший результат)
    Some(*latencies.iter().min().unwrap())
}
```

#### 2.5.4 Windows Polish (4-6 часов)
- [ ] Проверить все Windows функции вручную
- [ ] Добавить Windows-specific тесты
- [ ] Обработать edge cases (нет интернета, VPN, и т.д.)
- [ ] Обновить документацию
- [ ] Протестировать на Windows 10 и Windows 11

**Итого Phase 2.5:** ~24-30 часов (1 неделя)

---

## 📱 Expo для мобильных: Подробный анализ

### Что такое Expo?

**Expo** = React Native + batteries included

**Преимущества:**
- ✅ Быстрый старт (без Xcode/Android Studio)
- ✅ Over-the-air updates
- ✅ Много готовых модулей
- ✅ Одна кодовая база для iOS + Android
- ✅ TypeScript из коробки
- ✅ Хорошая документация

**Недостатки:**
- ❌ Ограничен модулями Expo (хотя можно добавить native)
- ❌ Нет прямого доступа к низкоуровневым API
- ❌ Больший размер APK/IPA
- ❌ Зависимость от Expo сервисов

### Expo для Netok: Feasibility Check

#### ✅ Что работает хорошо:
1. **UI переиспользование** - React код из `/ui` можно портировать
2. **Network info** - `expo-network` дает Wi-Fi SSID, IP, тип подключения
3. **Permissions** - `expo-permissions` для Wi-Fi доступа
4. **Updates** - OTA updates без App Store
5. **Локализация** - `expo-localization` + i18n

#### ⚠️ Что сложно:
1. **DNS изменение** - НЕТ API в Expo/React Native
   - На Android: нужен root или VPN API
   - На iOS: невозможно (ограничение Apple)
2. **Router info** - нет прямого API
3. **Low-level network** - требует native modules

#### ❌ Что невозможно:
- **Изменить DNS без root/VPN** (Android)
- **Изменить DNS вообще** (iOS - Apple запрет)
- **Gateway MAC lookup** (нет API)
- **Полная диагностика сети** (нужны native модули)

### Альтернативы для мобильных

#### 1. Expo + Custom Native Modules ⭐ (Рекомендуется)

**Архитектура:**
```
├── apps/
│   ├── desktop/          # Tauri (Windows/Linux/macOS)
│   └── mobile/           # Expo (iOS/Android)
│       ├── src/
│       │   ├── screens/ (переиспользовать из /ui)
│       │   └── native/  # Custom modules
│       └── ios/android/ # Native код
└── packages/
    └── netok_core/       # Rust shared logic (через FFI)
```

**Что нужно:**
- React Native bindings для netok_core (через FFI)
- Custom native modules для DNS (VPN API на Android)
- iOS: только диагностика (без изменения DNS)

**Сложность:** 🟡 Средняя (2-3 месяца)
**Стоимость:** $20,000-30,000

#### 2. Flutter + Rust FFI

**Плюсы:**
- ✅ Лучшая производительность
- ✅ flutter_rust_bridge хорошо работает
- ✅ Одна кодовая база

**Минусы:**
- ❌ Нужно переписать весь UI с React на Flutter
- ❌ Другой язык (Dart)
- ❌ Те же ограничения API

**Сложность:** 🔴 Высокая (3-4 месяца)
**Стоимость:** $30,000-40,000

#### 3. Tauri Mobile (v3 alpha) ⏳

**Плюсы:**
- ✅ Переиспользуем ВСЁ (Rust + React)
- ✅ Одна кодовая база для всех платформ
- ✅ Нулевая стоимость портирования

**Минусы:**
- ⏳ Alpha stage (не готово)
- ⏳ Релиз: Q2-Q3 2026 (примерно)
- ⚠️ Могут быть баги

**Сложность:** 🟢 Низкая (когда выйдет)
**Стоимость:** $0 (просто ждем)

---

## 🎯 Рекомендуемая Roadmap

### Этап 1: Windows Polish (1 неделя) ← СЕЙЧАС
- Router MAC lookup
- Router vendor (OUI)
- Internet latency
- Windows testing & polish

### Этап 2: Модульный рефакторинг (1 неделя)
- Разбить lib.rs на модули
- Подготовить архитектуру для других платформ

### Этап 3: Linux Support (2 недели)
- Wi-Fi через NetworkManager/nmcli
- DNS настройка
- Тестирование на Ubuntu/Fedora/Arch

### Этап 4: macOS Support (1.5 недели)
- Wi-Fi через airport/system_profiler
- DNS настройка
- Тестирование

### Этап 5: Phase 4 - Production Polish (2 недели)
- E2E тесты
- Security hardening
- Documentation
- Release automation

**Итого Desktop: ~7-8 недель**

### Этап 6 (опционально): Mobile

**Вариант A - Ждем Tauri 3.x** (0 работы, 6-12 месяцев)
- Просто обновить зависимости когда выйдет
- Добавить mobile targets в Cargo.toml

**Вариант B - Expo + Custom Modules** (~3 месяца)
- Создать Expo app
- Портировать UI
- Написать native modules для Android VPN DNS
- iOS: только read-only диагностика

---

## 💡 Моя рекомендация

### Краткосрочно (2-3 месяца):
1. ✅ **Windows на 100%** (Phase 2.5)
2. ✅ **Модульная архитектура** 
3. ✅ **Linux Wi-Fi**
4. ✅ **macOS Wi-Fi**
5. ✅ **Production ready desktop**

### Среднесрочно (6-12 месяцев):
1. ⏳ **Мониторить Tauri 3.x прогресс**
2. 📱 **Если срочно нужны mobile - Expo + native modules**
3. 🔄 **Накапливать фидбек от desktop юзеров**

### Долгосрочно (12+ месяцев):
1. 🚀 **Tauri 3.x stable → мобильная версия за неделю**
2. 📊 **Аналитика и метрики**
3. 🌍 **Больше языков**

---

## Вывод по Expo

### Expo подходит, если:
- ✅ Нужно быстро (2-3 месяца)
- ✅ Готовы писать native modules
- ✅ iOS: только диагностика (без изменения DNS)
- ✅ Android: VPN API для DNS (требует разрешение)

### Expo НЕ подходит, если:
- ❌ Хочется полный feature parity с desktop
- ❌ Нет времени на native modules
- ❌ Хочется минимальную кодовую базу

### Мой вердикт:
**Ждать Tauri 3.x** - это правильная стратегия:
- Меньше работы (0 vs 3 месяца)
- Одна кодовая база
- Полный feature parity
- Единая архитектура

**Но если нужно СЕЙЧАС** - Expo viable option с ограничениями.

